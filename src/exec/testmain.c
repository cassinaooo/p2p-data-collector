#include "tester.h"

/**
 * este eh o ponto de entrada para a aplicacao tester
 * esse arquivo eh "colado" em cima dos binarios para ser um main, a depender da aplicacao que se deseja construir
 * veja Makefile
 */

int main(){
    test();
    return 0;
}